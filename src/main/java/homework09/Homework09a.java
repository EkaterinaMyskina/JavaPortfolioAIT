/*
Задание: Калькулятор операций

Цель: Разработать программу на Java, которая использует циклы и условные операторы для
выполнения арифметических операций с вводимыми пользователем числами. Программа должна
предложить пользователю выбрать операцию (сложение, вычитание, умножение, деление)
и ввести два числа, с которыми будет проведена операция. После вывода результата программа
должна спросить пользователя, хочет ли он продолжить выполнение операций. Если пользователь
отвечает утвердительно, программа должна снова предложить выбрать операцию и ввести числа.
Часть 1: Ввод данных

    Запросить у пользователя выбор операции.
    Запросить у пользователя ввод двух чисел.

Часть 2: Выполнение операции

    Использовать условные операторы (if, else if, else) для определения выбранной операции.
    Выполнить выбранную арифметическую операцию с введенными числами.
    Вывести результат операции.

Часть 3: Цикл выполнения

    Использовать цикл (while или do-while), чтобы повторять выполнение операций, пока
    пользователь не решит выйти. После вывода результата спросить пользователя, хочет ли он
    продолжить выполнение операций. Если пользователь отвечает утвердительно, повторить
    процесс. Если отрицательно — завершить программу.

Дополнительное условие

    Убедитесь, что программа обрабатывает деление на ноль, выводя сообщение об ошибке и
    предлагая пользователю повторить ввод чисел для операции.

Давайте создадим более сложное задание, которое будет включать работу с циклами и условными
операторами в Java, углубляя понимание этих конструкций и требуя от студентов более сложного
решения без использования массивов.
 */
package homework09;

import java.util.Scanner;

public class Homework09a {
/*
Запросить у пользователя выбор операции:
сложение, вычитание, умножение, деление.
Запросить у пользователя ввод двух чисел.
*/

    // Игорь, код немного поправила. Он сложносочиненный, но по минимуму все работает (без проверок на неверный ввод).
    public static void main(String[] args) {
        boolean exit = false;
        double result = 0;

        do {
            Scanner scanner = new Scanner(System.in);

            System.out.println(" Выберите операцию: +, -, *, /. ");
            String operation = scanner.next();

            System.out.println("Введите число 1:");
            double numberOne = scanner.nextDouble();

            System.out.println("Введите число 2:");
            double numberTwo = scanner.nextDouble();

            if (operation.equals("+")) {    // сложение работает
                result = numberOne + numberTwo;
                System.out.println("Результат операции " + operation + " = " + result);
            }
            if (operation.equals("-")) {          //вычитание работает
                result = numberOne - numberTwo;
                System.out.println("Результат операции " + operation + " = " + result);
            }
            if (operation.equals("*")) {              // умножение работает
                result = numberOne * numberTwo;
                System.out.println("Результат операции " + operation + " = " + result);
            }


            if ((operation.equals("/") && (numberTwo != 0))) {  // деление работает
                result = numberOne / numberTwo;
                System.out.println("Результат операции " + operation + " = " + result);
            } else {
                if ((operation.equals("/") && (numberTwo == 0))) {
                    System.out.println("Деление на 0 невозможно. ");
                }
            }

            System.out.println("Вы хотите продолжить расчеты? Введите да или нет: ");
            String userAnswer = scanner.next();
            if (userAnswer.equals("да")) {
                exit = false;
            } else {
                System.out.println("Программа завершена. Спасибо за участие.");
                exit = true;
                scanner.close();
            }
        }
        while (!exit);
    }
}